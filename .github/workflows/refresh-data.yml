name: Refresh Score Data

on:
  schedule:
    # Wednesdays and Saturdays at 06:17 UTC
    - cron: "17 6 * * 3,6"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: refresh-score-data
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    env:
      RELEASE_PAT: ${{ secrets.RELEASE_PAT }}
      WCL_CLIENT_ID: ${{ secrets.WCL_CLIENT_ID }}
      WCL_CLIENT_SECRET: ${{ secrets.WCL_CLIENT_SECRET }}
      WCL_ZONE_IDS: ${{ vars.WCL_ZONE_IDS }}
      WCL_ZONE_ID: ${{ vars.WCL_ZONE_ID }}
      WCL_METRIC: ${{ vars.WCL_METRIC || 'dps' }}
      WCL_MAX_GUILDS: ${{ vars.WCL_MAX_GUILDS || '1000' }}
      WCL_GRAPHQL_URL: ${{ vars.WCL_GRAPHQL_URL || 'https://www.warcraftlogs.com/api/v2/client' }}
      LFR_WCL_DISTRIBUTION_APPROVED: ${{ vars.LFR_WCL_DISTRIBUTION_APPROVED }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ env.RELEASE_PAT != '' && env.RELEASE_PAT || github.token }}

      - name: Check redistribution approval
        id: gate
        shell: bash
        run: |
          if [[ "${LFR_WCL_DISTRIBUTION_APPROVED}" == "true" ]]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          echo "::warning::LFR_WCL_DISTRIBUTION_APPROVED is not true. Skipping until redistribution permission is confirmed."
          echo "run=false" >> "$GITHUB_OUTPUT"

      - name: Collect guild list via WCL API
        if: steps.gate.outputs.run == 'true'
        shell: bash
        run: |
          set -euo pipefail
          [[ -n "${WCL_CLIENT_ID}" ]] || { echo "WCL_CLIENT_ID secret is required" >&2; exit 1; }
          [[ -n "${WCL_CLIENT_SECRET}" ]] || { echo "WCL_CLIENT_SECRET secret is required" >&2; exit 1; }
          [[ -n "${WCL_ZONE_IDS}" || -n "${WCL_ZONE_ID}" ]] || { echo "WCL_ZONE_IDS or WCL_ZONE_ID repository variable is required" >&2; exit 1; }
          python3 tools/fetch_wcl_scores.py \
            --realms data/realms.json \
            --output data/scores.json \
            --state-file data/fetch_state.json \
            --zone-ids "${WCL_ZONE_IDS:-${WCL_ZONE_ID}}" \
            --metric "${WCL_METRIC}" \
            --max-guilds "${WCL_MAX_GUILDS}" \
            --guild-list-only \
            --distribution-approved

      - name: Install Playwright and Chromium
        if: steps.gate.outputs.run == 'true'
        shell: bash
        run: |
          set -euo pipefail
          pip install playwright
          playwright install chromium --with-deps

      - name: Scrape guild scores
        if: steps.gate.outputs.run == 'true'
        shell: bash
        run: |
          set -euo pipefail
          python3 tools/scrape_scores.py \
            --state-file data/fetch_state.json \
            --output data/scores.json \
            --max-guilds "${WCL_MAX_GUILDS}" \
            --failed-guilds data/retry_guilds.json

      - name: Commit initial scan results
        if: steps.gate.outputs.run == 'true'
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/scores.json data/fetch_state.json
          # Stage retry list only when guilds failed (file only written when non-empty)
          if [[ -f data/retry_guilds.json ]]; then
            git add data/retry_guilds.json
          fi
          if git diff --cached --quiet; then
            echo "No data changes, nothing to commit."
          else
            git commit -m "chore(data): scrape initial score data"
            git push
          fi
